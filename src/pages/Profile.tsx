import { User } from '@/components/User';

export const ProfilePage = () => {
  return (
    <div className="flex -mt-24 flex-col min-h-screen items-center justify-center">
      <User />
    </div>
  );
};
